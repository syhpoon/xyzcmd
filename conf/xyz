#
# Main configuration file. Default settings. Do not edit.
#

# Enabled plugins
# Syntax is:
#   {plugin_ns_path} > {ENABLE | DISABLE}

plugins {
	:sys:run > ENABLE
	:sys:cmd > ENABLE
	:sys:panel > ENABLE
	:sys:logger > ENABLE

	:core:keycodes > ENABLE
	:core:pluginlist > ENABLE
	:core:bindlist > ENABLE

	:misc:hello > ENABLE
	:misc:about > ENABLE

	:ui:testinput > ENABLE
	:ui:bookmarks > ENABLE
}

# Skin to use
skin = seablue

# Local encoding
# One of the available in python
# (http://docs.python.org/lib/standard-encodings.html)
local_encoding = "utf-8"
