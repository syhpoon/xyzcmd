#
# Main configuration file
#

# Enabled plugins
# Syntax is:
#   {plugin_ns_path} > {ENABLE | DISABLE}

plugins {
	:misc:hello > ENABLE
	:core:keycodes > ENABLE
	:core:pluginlist > ENABLE
	:ui:testinput > ENABLE
}

# Skin to use
skin = monochrome
#skin = seablue

# Command line prompt
# TODO: macros in prompt
# TODO: Test no prompt cmd
cmd_prompt = "$"
