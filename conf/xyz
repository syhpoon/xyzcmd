#
# Main configuration file
#

# Enabled plugins
# Syntax is:
#   {plugin_ns_path} > {ENABLE | DISABLE}

plugins {
	:misc:hello > ENABLE
	:core:keycodes > ENABLE
	:core:pluginlist > ENABLE
	:core:logger > ENABLE
}

# Skin to use
#skin = monochrome
skin = seablue
