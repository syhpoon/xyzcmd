#
# Main configuration file
#

# Enabled plugins
# Syntax is:
#   {plugin_ns_path} > {ENABLE | DISABLE}

plugins {
	:misc:hello > ENABLE
	:core:keycodes > ENABLE
	:core:pluginlist > ENABLE
}

# Skin to use
skin = monochrome
#skin = seablue

# Log levels to track
log_level = INFO,DEBUG,NONE,ALL,NONE

# Maximum number of entries to keep
log_lines = 100
