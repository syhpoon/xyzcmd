#
# Main configuration file
#

# Enabled plugins
# Syntax is:
#   {plugin_ns_path} > {ENABLE | DISABLE}

plugins {
	:misc:hello > ENABLE
	:core:keycodes > ENABLE
	:core:pluginlist > ENABLE
	:core:bindlist > ENABLE
	:ui:testinput > ENABLE
	:misc:about > ENABLE
}

# Skin to use
skin = glamour

# Command line prompt
# TODO: macros in prompt
cmd_prompt = "|| "

# Local encoding
#local_encoding = "koi8-r"
