AUTHOR: "Max E. Kuznecov <syhpoon@syhpoon.name>"
VERSION: "0.2"
DESCRIPTION: "XYZCommander Strawberry skin"

# Filesystem objects
fs.rules {
	&BG = DARK_MAGENTA

	"type{file} and perm{+0111}" = LIGHT_GREEN, &BG

	'''regexp{".*\\.core$"}''' = DARK_RED, &BG
	'''regexp{"^\\.{1}[^.]"}''' = DARK_GRAY, &BG

	"type{file}" = LIGHT_GRAY, &BG
	"type{dir}" = WHITE, &BG
	"type{link}" = LIGHT_CYAN, &BG
	"type{socket}" = BLACK, &BG
	"type{fifo}" = LIGHT_BLUE, &BG
	"type{char}" = DARK_GRAY, &BG
	"type{block}" = LIGHT_MAGENTA, &BG
}

### User interface objects

# Base widget
ui.widget {
	button = DARK_MAGENTA, LIGHT_GRAY
}

# Base box widgets
ui.box {
	&BG = LIGHT_GRAY

	title = &BG, DARK_MAGENTA
	box = BLACK, &BG
	border = DARK_MAGENTA, &BG
	mount = DEFAULT, &BG
}

# Error message box
ui.error_box {
	&FG = WHITE
	&BG = DARK_RED

	title = &FG, BLACK
	box = &FG, &BG
	border = &FG, &BG
	mount = &FG, &BG
}

# Input box
ui.input_box {
	input = WHITE, DARK_MAGENTA
}

# Panel widget
ui.panel {
	&BG = DARK_MAGENTA

	cwdtitle = BLACK, LIGHT_GRAY
	cwdtitleinact = WHITE, &BG
	panel = LIGHT_GRAY, &BG
	cursor = BLACK, LIGHT_GRAY
	info = WHITE, &BG
	border = WHITE, &BG
	tagged = YELLOW, &BG
}

# XYZListBox
ui.list_box {
	title = DARK_MAGENTA, LIGHT_GRAY
	selected = WHITE, DARK_MAGENTA
}

# Command line widget
ui.cmd {
	&BG = DARK_MAGENTA

	prompt = WHITE, &BG
	text = LIGHT_GRAY, &BG
}
