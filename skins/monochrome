# Default XYZ skin

AUTHOR: "Max E. Kuznecov <syhpoon@syhpoon.name>"
VERSION: "0.1"
DESCRIPTION: "XYZCommander monochrome skin"

### User interface objects

# Base widget
ui.widget {
	button = BLACK, LIGHT_GRAY
}

# Base box widgets
ui.box {
	&BG = LIGHT_GRAY

	title = WHITE, DEFAULT
	box = BLACK, &BG
	border = BLACK, &BG
	mount = DEFAULT, &BG
        input = &BG, DEFAULT
}

# Error message box
ui.error_box {
	&BG = DARK_RED

	title = WHITE, BLACK
	box = WHITE, &BG
	border = WHITE, &BG
	mount = WHITE, &BG
}

# Input box
ui.input_box {
	input = WHITE, DEFAULT
}

ui.panel {
	&BG = LIGHT_GRAY

	cwdtitle = BLACK, &BG
	cwdtitleinact = WHITE, DEFAULT
	panel = DEFAULT, DEFAULT
	cursor = BLACK, &BG
	search = WHITE, &BG
	info = WHITE, DEFAULT
	border = DEFAULT, DEFAULT
	tagged = WHITE, &BG
}

# XYZListBox
ui.list_box {
	title = BLACK, LIGHT_GRAY
	selected = WHITE, DEFAULT
}

# Command line widget
ui.cmd {
	prompt = WHITE, DEFAULT
	text = LIGHT_GRAY, DEFAULT
}

# Settings for :core:console
plugin.console
{
        input = BLACK, LIGHT_GRAY
        header = BLACK, LIGHT_GRAY
        output = WHITE, DEFAULT

}
